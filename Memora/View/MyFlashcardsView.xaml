<UserControl x:Class="Memora.View.MyFlashcardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Memora.View"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"></RowDefinition>
            <RowDefinition Height="0.1*"></RowDefinition>
            <RowDefinition Height="0.8*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0"
               Content="My Flashcards view"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontSize="48"
               FontFamily="Calibri"
               Foreground="WhiteSmoke"></Label>

        <Label Grid.Row="1"
               Content="Flashcard study sets"
               FontSize="45"
               HorizontalAlignment="Center"></Label>
        
        <ListView Grid.Row="2"
                  x:Name="FlashcardSetList" 
                  ItemsSource="{Binding FlashcardSets}"
                  Height="400"
                  Width="400"
                  Margin="15"
                  ItemContainerStyle="{StaticResource FlashcardSetDesign}">

        </ListView>

    </Grid>
</UserControl>
