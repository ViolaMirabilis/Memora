<UserControl x:Class="Memora.View.QuizModeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Memora.View"
      mc:Ignorable="d">

    <Grid Background="Transparent">

        <ScrollViewer Width="500">
            <ItemsControl ItemsSource="{Binding QuizAnswers}"
              x:Name="FlashcardList" 
                Background="Transparent"
                BorderThickness="0"
                Height="auto"
                Width="auto"
                Margin="15,15,15,15"
                HorizontalAlignment="Center"
                HorizontalContentAlignment="Center"
                ScrollViewer.CanContentScroll="True"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="MediumPurple"
                        BorderThickness="2"
                        CornerRadius="10"
                        Margin="5"
                        Padding="10">

                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Question}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="38"
                               FontWeight="SemiBold"
                               Margin="15"
                               TextWrapping="Wrap">

                                </TextBlock>
                                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Button Height="50" Width="250" Content="{Binding IncorrectAnswers[0]}" FontSize="14" Margin="10"/>
                                    <Button Height="50" Width="250" Content="{Binding IncorrectAnswers[1]}" FontSize="14" Margin="10"/>
                                    <Button Height="50" Width="250" Content="{Binding IncorrectAnswers[2]}" FontSize="14" Margin="10"/>
                                    <Button Height="50" Width="250" Content="{Binding IncorrectAnswers[3]}" FontSize="14" Margin="10"/>
                                </StackPanel>
                            </StackPanel>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
    </Grid>
</UserControl>
