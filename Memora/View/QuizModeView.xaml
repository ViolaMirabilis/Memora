<UserControl x:Class="Memora.View.QuizModeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:Memora.View"
      mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/StudyModeButton.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="Transparent">

        <ScrollViewer Width="auto">
            <StackPanel Orientation="Vertical">
                <ItemsControl ItemsSource="{Binding QuizAnswers}"
                              x:Name="FlashcardList" 
                                Background="Transparent"
                                BorderThickness="0"
                                Height="auto"
                                Width="auto"
                                Margin="15,15,15,15"
                                HorizontalAlignment="Center"
                                HorizontalContentAlignment="Center"
                                ScrollViewer.CanContentScroll="True"
                                ScrollViewer.VerticalScrollBarVisibility="Visible"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="2"
                                    CornerRadius="10"
                                    Margin="5"
                                    Padding="10"
                                    BorderBrush="#DEE2E6"
                                    Background="#6366F1"
                                    Width="auto">

                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Question}"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Center"
                                               FontSize="24"
                                               FontWeight="SemiBold"
                                               Margin="10"
                                               TextWrapping="Wrap"
                                               Foreground="#FFFFFF">

                                    </TextBlock>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Border RenderOptions.EdgeMode="Aliased" CornerRadius="10" Background="#4F46E5" BorderBrush="#4F46E5" BorderThickness="2" Margin="10">
                                                <ToggleButton Padding="5,0,0,0" HorizontalContentAlignment="Left" Background="Transparent" Foreground="#FFFFFF" Height="35" Width="250" Content="{Binding IncorrectAnswers[0]}" FontSize="18" BorderThickness="0"/>
                                            </Border>

                                            <Border RenderOptions.EdgeMode="Aliased" CornerRadius="10" Background="#4F46E5" BorderBrush="#4F46E5" BorderThickness="2" Margin="10">
                                                <ToggleButton Padding="5,0,0,0" HorizontalContentAlignment="Left" Background="Transparent" Foreground="#FFFFFF" Height="35" Width="250" Content="{Binding IncorrectAnswers[1]}" FontSize="18" BorderThickness="0"/>
                                            </Border>

                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Border RenderOptions.EdgeMode="Aliased" CornerRadius="10" Background="#4F46E5" BorderBrush="#4F46E5" BorderThickness="2" Margin="10">
                                                <ToggleButton Padding="5,0,0,0" HorizontalContentAlignment="Left" Background="Transparent" Foreground="#FFFFFF" Height="35" Width="250" Content="{Binding IncorrectAnswers[2]}" FontSize="18" BorderThickness="0"/>
                                            </Border>

                                            <Border RenderOptions.EdgeMode="Aliased" CornerRadius="10" Background="#4F46E5" BorderBrush="#4F46E5" BorderThickness="2" Margin="10">
                                                <ToggleButton Padding="5,0,0,0" HorizontalContentAlignment="Left" Background="Transparent" Foreground="#FFFFFF" Height="35" Width="250" Content="{Binding IncorrectAnswers[3]}" FontSize="18" BorderThickness="0"/>
                                            </Border>

                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>

                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
                <!-- submit button appearing AFTER the last answer -->
                <Button Content="Check answers"
                        Style="{StaticResource StudyModeButton}"
                        Tag="{x:Static iconPacks:PackIconBootstrapIconsKind.Check2Square}"
                        Command="{Binding CheckAnswersCommand}"
                        FontWeight="SemiBold"
                        Width="155"
                        Height="55"
                        Margin="5"/>
            </StackPanel>
            
        </ScrollViewer>
    </Grid>
</UserControl>
