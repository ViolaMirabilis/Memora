<UserControl x:Class="Memora.View.QuizModeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:Memora.View"
      mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/StudyModeButton.xaml"/>
                <ResourceDictionary Source="/Themes/QuizQuestionButton.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="Transparent">

        <ScrollViewer Width="auto">
            <StackPanel Orientation="Vertical">
                <ItemsControl x:Name="FlashcardList" 
                              ItemsSource="{Binding QuizAnswers}"
                                Background="Transparent"
                                BorderThickness="0"
                                Height="auto"
                                Width="auto"
                                Margin="15,15,15,15"
                                HorizontalAlignment="Center"
                                HorizontalContentAlignment="Center"
                                ScrollViewer.CanContentScroll="True"
                                ScrollViewer.VerticalScrollBarVisibility="Visible"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="2"
                                    CornerRadius="10"
                                    Margin="5"
                                    Padding="10"
                                    BorderBrush="#DEE2E6"
                                    Background="#6366F1"
                                    Width="450"
                                    Height="auto">

                                <StackPanel Orientation="Vertical">
                                    <TextBlock Margin="2"
                                               Foreground="#FFFFFF"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Right">
                                        <Run Text="{Binding Index}"/>
                                        <Run Text="/"/>
                                        <!-- going up the visual tree, grabbing the QuizModeViewModel and OneWay binding, which is for display only.-->
                                        <Run Text="{Binding DataContext.TotalFlashcards, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}"/>

                                    </TextBlock>
                                    <TextBlock Text="This is a template question to see how the control behaves if the text is way too long."
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Center"
                                               FontSize="20"
                                               FontWeight="Regular"
                                               Margin="10,2,10,10"
                                               TextWrapping="Wrap"
                                               TextAlignment="Justify"
                                               Foreground="#FFFFFF">

                                    </TextBlock>
                                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <RadioButton Style="{StaticResource QuizAnswer}" Content="{Binding IncorrectAnswers[0]}"/>
                                        <RadioButton Style="{StaticResource QuizAnswer}" Content="{Binding IncorrectAnswers[1]}"/>
                                        <RadioButton Style="{StaticResource QuizAnswer}" Content="{Binding IncorrectAnswers[2]}"/>
                                        <RadioButton Style="{StaticResource QuizAnswer}" Content="{Binding IncorrectAnswers[3]}"/>

                                    </StackPanel>
                                </StackPanel>

                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
                <!-- submit button appearing AFTER the last answer -->
                <Button Content="Check answers"
                        Style="{StaticResource StudyModeButton}"
                        Tag="{x:Static iconPacks:PackIconBootstrapIconsKind.Check2Square}"
                        Command="{Binding CheckAnswersCommand}"
                        FontWeight="SemiBold"
                        Width="155"
                        Height="55"
                        Margin="10,10,10,20"/>
            </StackPanel>
            
        </ScrollViewer>
    </Grid>
</UserControl>
